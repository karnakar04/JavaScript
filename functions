FUNCTIONS:
A function is reusable block of code
It can be call any where program
--Reusable code: using an existing code with out writing  it ever we need
You can the same code with different arguments to get the results

JS:
Key word= function
( )
Parameters: values(var) which are declare inside the () While definig a function
Arguments: Values which are passed through the parameters at the time of calling the function
Syntax:
Function greet(name)
#Error
Example:
function greet(){
    console.log("HI ramulu")
}
greet()

Ex2:
function greet(){
    console.log("HI ramulu")
}
greet() //hi ramulu
console.log("Hello world") //hello world
greet() //hi ramulu

Example: FUNCTION WITH PARAMETERS:
function g(name){
    console.log("hello "+name)

}
let name = prompt("Enter your name:")

g(name)


Ex3:
function add(a,b){
    document.write(a+b)
}
add(1,1)

function return:
--The return statement can be used 

Example:
function a(aa,bb){
    return aa+bb
}
let n1=parseFloat(prompt("Enter a number: "))
let n2=parseFloat(prompt("Enter b number: "))

//calling a function

let result =a(n1,n2)
//displaying the result
document.write("The Sum is: ",result)

Anonymous function: 
A function with out name , After we create a function with out name and we assign it to a variable
Example:
sum=function(n1,n2){
    let r=n1+n2
    return r
}
document.write(sum(10,2))

Q1.Write a program to check give number is prime or not
0 1 1 2 3 5 8 13 =fibno
Leap year
Armstrong number  1 2 3==1^3+2^3+3^3
Palindron  # 121     ==   121 
# pattern


